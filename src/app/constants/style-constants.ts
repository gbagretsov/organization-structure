// All constants that are use for calculation of linking line position
// should be synced with style-constants.scss

const StyleConstants = {
  cellWidth: 180,
  cellHeight: 90,
  linkingLineWidth: 4,
  gapBetweenCells: 16,
}

export default StyleConstants;
